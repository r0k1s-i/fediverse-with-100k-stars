
import { gup } from '../utils/misc.js';

export const GITHUB_REPO = 'r0k1s-i/fediverse-with-100k-stars';
export const fediverseDataPath = `https://cdn.jsdelivr.net/gh/${GITHUB_REPO}@data/fediverse_final.json`;

window.fediverseDataPath = fediverseDataPath;

